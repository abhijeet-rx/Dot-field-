/**
 * Mapper layer — entity ↔ DTO conversion.
 * <p>
 * Mappers translate between JPA entities and DTOs to keep
 * the API boundary clean and decoupled from persistence.
 */
package com.dotfield.mapper;
